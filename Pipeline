pipeline {
    agent any

    stages {

        stage('Clone') {
            steps {
                git 'https://github.com/your-username/university-department-webpage.git'
            }
        }

        stage('Build Docker Image') {
            steps {
                sh 'docker build -t university-web .'
            }
        }

        stage('Run Container') {
            steps {
                sh 'docker run -d -p 8080:80 university-web'
            }
        }

    }
}
